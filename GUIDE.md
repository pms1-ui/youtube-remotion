# 헬마드 영상 제작 가이드

## 개요
Remotion 기반 스크립트→영상 자동 생성 시스템.
스크립트 데이터만 수정하면 16:9 까만 배경의 인포그래픽 영상이 생성됩니다.

---

## 디자인 원칙

### 레이아웃
- **모든 요소 가운데 정렬** (수직/수평 모두)
- 16:9 비율 (1920×1080)
- 까만 배경 (#0a0a0a)
- 장면 전환: 페이드 인/아웃

### ❌ 사용 금지
- **상단 뱃지/태그 금지** — "30대의 변화", "운동의 영역" 같은 상단 카테고리 뱃지 사용하지 않음
- **반복 아이콘 금지** — 같은 아이콘이 여러 장면에 반복되면 안 됨
- **장식용 소형 텍스트 금지** — 읽히지 않는 작은 글씨는 넣지 않음

### 최소 폰트 사이즈 (절대 기준)
| 요소 | 최소 사이즈 | 권장 사이즈 |
|------|:----------:|:----------:|
| 인트로 타이틀 | 120px | 140px |
| 장면 메인 텍스트 | 50px | 54~64px |
| 차트 X축/Y축 라벨 | 26px | 28~32px |
| 차트 값 숫자 | 28px | 30~40px |
| 범례 라벨 | 34px | 38px |
| 범례 값 | 36px | 40px |
| 프로그레스 내부 숫자 | 34px | 38px |
| 프로그레스 하단 라벨 | 30px | 34px |

**규칙: 화면에 표시되는 어떤 텍스트도 26px 미만이면 안 됩니다.**

### 그래픽 요소 최소 사이즈
| 요소 | 최소 사이즈 |
|------|:----------:|
| 도넛 차트 | 직경 360px |
| 원형 프로그레스 | 직경 180px |
| 막대 차트 전체 영역 | 1200×480px |
| 라인 그래프 전체 영역 | 1150×440px |
| 도넛 선 두께 | 36px |
| 프로그레스 선 두께 | 12px |

### 시각 요소 우선 사용 순서
1. **차트/그래프** (막대, 라인, 도넛) — 수치 데이터가 있을 때 필수
2. **원형 프로그레스** — 비율/달성도 표현
3. **텍스트만** — 차트로 표현 불가한 메시지일 때만

---

## 장면 타입 (SceneType)

### `intro` / `outro`
- 채널명 + 인사 메시지
- 큰 타이틀 (140px) + 구분선 + 부제목 (56px)
- 뱃지 없음

### `text`
- 핵심 메시지 전달 (차트 불필요한 내용)
- 악센트 라인 + 가운데 텍스트 (64px)
- 뱃지 없음

### `barChart`
- **막대 차트** — SVG로 렌더링
- `barData` 필수: `{ label, value, color }[]`
- 상단: 메인 텍스트 (54px), 뱃지 없음
- 바가 아래에서 올라오는 spring 애니메이션
- X축 라벨 32px, 값 34px, Y축 28px

### `donutChart`
- **도넛 차트** — SVG 원형
- `donutData` 필수: `{ label, value, color }[]`
- 상단: 메인 텍스트 (54px), 뱃지 없음
- 각 세그먼트가 순차적으로 그려지는 애니메이션
- 우측 범례: 라벨 38px, 값 40px

### `lineGraph`
- **라인 그래프** — SVG 베지어 곡선
- `lineData` 필수: `{ label, value }[]`
- 상단: 메인 텍스트 (50px), 뱃지 없음
- `evolvePath`로 선이 왼→오 그려지는 애니메이션
- X축 라벨 28px, 값 30px, Y축 26px

### `highlight`
- 강조 메시지 (62px) + 원형 프로그레스 바 (180px)
- `bullets` 필수: 하단에 프로그레스 지표로 표시
- 뱃지 없음

---

## 스크립트 작성법

`src/data/script.ts`의 `SCENES` 배열을 수정합니다.

```typescript
{
  type: "barChart",          // 장면 타입
  text: "메인 설명 텍스트",   // 화면 상단 텍스트
  durationInSeconds: 10,    // 장면 길이 (초)
  accent: "#ff6b6b",        // 테마 컬러
  barData: [                // 차트 데이터
    { label: "항목", value: 85, color: "#ff6b6b" },
  ],
}
```

> ⚠️ `title` 필드는 인트로에서만 사용합니다. 다른 장면에서는 뱃지로 표시되므로 사용하지 마세요.

### 데이터 형식

**barData** (막대 차트):
```typescript
{ label: string; value: number; color: string }[]
// value는 0~100 사이 권장 (최대값 기준으로 자동 스케일)
```

**donutData** (도넛 차트):
```typescript
{ label: string; value: number; color: string }[]
// value 합계가 전체 비율을 나타냄
```

**lineData** (라인 그래프):
```typescript
{ label: string; value: number }[]
// accent 색상이 라인에 적용됨
```

---

## 실행 명령어

```bash
npm start          # Remotion Studio (localhost:3000에서 미리보기)
npm run render     # MP4로 렌더링 → out/video.mp4
```

---

## 색상 팔레트 추천

| 용도 | 색상 |
|------|------|
| 긍정/성장 | #00b894, #55efc4, #00cec9 |
| 경고/감소 | #e17055, #d63031, #ff7675 |
| 강조/하이라이트 | #ffd93d, #fdcb6e, #f39c12 |
| 정보/안내 | #6c5ce7, #a29bfe, #74b9ff |
| 건강/체력 | #00e5ff, #48dbfb, #4ecdc4 |
| 위험/주의 | #fd79a8, #e056fd, #ff9ff3 |
